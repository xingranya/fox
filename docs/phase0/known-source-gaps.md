# Phase 0 已知资料缺口

## 当前结论

当前授权材料只能证明已完成 `example/` 的只读盘点与本地解析，不能证明完整鸿日/V5 基线已获得或完成对账。需求说明中出现的资料数量与分段数量均按 `declared_unverified` 处理，不转写为已验证事实。

## 缺口清单

| 缺口 ID | 状态 | 缺失内容 | 当前影响 | 允许动作 | 禁止表述 |
|:---|:---|:---|:---|:---|:---|
| `GAP-001` | `PARTIALLY_RESOLVED` | 已获得远程完整资料根和当前入口，但约 14GB 全量物理文件尚未完成对账，部分目录无读取权限 | 当前工作可按 9 份 SSOT/工作源装配；仍不能宣称全库逐文件验证完成 | 使用当前来源继续 Phase 0；另行处理权限与全量 Manifest | “已完成全部远程资料全量对账” |
| `GAP-002` | `PARTIALLY_RESOLVED` | 项目已定义来源角色、SSOT、五态和历史覆盖；并非所有原件都已逐项映射 | 可以建立当前状态 Task Packet，但不能把未登记文件自动视为 current | 按项目总控、资源清单和决策日志装配，历史默认不加载 | “远程目录中的文件全部当前有效” |
| `GAP-003` | `PARTIALLY_RESOLVED` | 已找到 Fox 决策日志及 `confirmed_by_fox` 记录，但尚未导入 Brand Project OS 正式事件存储 | 可用于评审和契约验证，不能在尚无核心实现时宣称已完成正式状态迁移 | 保留来源哈希、状态和范围，Phase 1 再做事件导入与对账 | “远程决策已全部迁移并生效” |
| `GAP-004` | `KNOWN_SOURCE_GAP` | 会议音频与人工校对转写、说话人、时间位置 | 无法把模型转写作为正式原话证据 | 保留模型候选转写及不确定性 | “模型转写等于会议原话” |
| `GAP-005` | `KNOWN_SOURCE_GAP` | 普通聊天 BrandBench 基线与 Fox 匿名评分 | F0.6 和 F0.7 阶段门不能通过 | 先准备评分表与匿名化流程，等待 Fox 评分 | “品牌质量已达到基线” |

## 已声明但未核验的数据

- 需求说明中关于 V5 原件数量、内容分段数量或历史处理规模的描述，只作为规划输入保存。
- 在获得对应原件、Manifest 和人工确认前，其证据状态统一为 `declared_unverified`。
- `declared_unverified` 不得进入正式事实投影，也不得用于对领导宣称系统已经完成真实全量接入。

## 解除缺口所需最小输入

当前已获得远程资料入口。后续还需要以下最小输入：

1. 处理当前无权限目录，或明确将其排除在首轮全量对账之外。
2. 明确远程资料中允许进入外部模型的级别；当前默认只做本地读取。
3. Phase 1 建立事件存储后，选择一组 `confirmed_by_fox` 决策做首批导入与人工复核。
4. 对新的 BrandBench 匿名候选完成一次人工评分。

在以上输入到位前，系统继续使用脱敏样本完成 Schema、Fixture、测试和 OpenWork 接入准备，但不越过 Phase 0 实施就绪门。
