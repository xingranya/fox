# Phase 1：单一客户端本地纵向切片

**目标**：让 Fox 在公司定制 OpenWork 中完成资料、会议变化、当前状态、AI 工作、Proposal、人工确认和证据回查，并冻结服务器迁移基线。
**状态**：进行中
**任务数**：10
**自适应阈值**：标注 2 / 重计划 4 / 重定范围 6

## 任务

- [x] **F1.1：建立可配置本地工作空间、只读原件库、配置与测试骨架**
  - 优先级 / 工作量 / Lane：P0 / M / A
  - 依赖：F0.7
  - S.U.P.E.R：S、E、R
  - 测试期望：临时目录、配置优先级、只读、路径逃逸、哈希和备份/恢复烟测。
  - 治理影响：固化本地目录、密钥、原件和派生文件分区规则。
  - 验收：路径可配置；原件只读、派生数据可重建、状态库可备份；密钥不进仓库/日志；无服务器或 Docker 前置。
  - 备注：2026-07-22 完成。建立 `src/brand_os` Python 工程骨架、四级配置加载、`.fox` 六分区、只读内容寻址证据快照及状态目录备份恢复；原件越界、符号链接、快照篡改和备份篡改均有测试。14 项 F1.1 测试通过，统一 `uv run pytest` 共 34 项通过；未启动常驻服务。

- [x] **F1.2：实现轻量 SQLite Schema/迁移、关系、事件、投影与本地备份**
  - 优先级 / 工作量 / Lane：P0 / L / A
  - 依赖：F1.1、F0.3
  - S.U.P.E.R：S、U、P、R
  - 测试期望：迁移升级/回退、事务回滚、约束、事件重放、备份恢复和 SQLite 版本测试。
  - 治理影响：固化单用户本地权威与未来存储 Port。
  - 验收：保存来源、候选、确认事件、当前状态、关系和 Proposal；事件与投影原子提交；Schema 版本清晰，未来换存储只改适配器。
  - 备注：2026-07-22 完成。实现 SQLite Schema v2、事务内事件与投影、幂等键、预期版本、Fox 人工评审门、事件重放、投影重建和 SQLite 在线备份恢复。AI/工作流只能写候选与 Proposal，不能批准；27 项相关新测试、61 项全量测试通过，`ruff` 与编译检查通过。F1.3-F1.5 按任务需要把端口逐步升级到 `canonical-store-port.v4`，原有命令语义不变。

- [x] **F1.3：实现鸿日资料幂等导入、V5 旧 ID/版本/缺口/去重对账**
  - 优先级 / 工作量 / Lane：P0 / M / A
  - 依赖：F1.2、F0.2
  - S.U.P.E.R：S、U、P
  - 测试期望：重复导入、坏行、旧 ID、废弃保号、哈希变化、数量和缺口金标测试。
  - 治理影响：记录导入映射与差异，禁止缺失补写。
  - 验收：重复 Manifest 不新增对象；V5 各层数量/关系对账；旧 ID、废弃保号、版本和 `KNOWN_SOURCE_GAP` 保留；内容变化形成新版本。
  - 备注：2026-07-22 完成。实现 `source-import.v1`、现有两种 Manifest 适配、SQLite v3 版本表、旧 ID/废弃保号、缺口、内容去重、哈希变更和整批回滚。真实 9 条远程来源首轮得到 9 个逻辑来源、9 个内容对象、9 个版本和 5 条缺口，第二轮所有表新增 0 行，正式业务状态仍为 0。V5 无对应 Manifest，只保留已知缺口；79 项全量测试、`ruff`、编译和 SQLite `quick_check` 通过。

- [x] **F1.4：实现会议增量摄取、原话分段、模式识别、分类与冲突候选**
  - 优先级 / 工作量 / Lane：P0 / L / B
  - 依赖：F1.3、F0.3
  - S.U.P.E.R：S、U、P
  - 测试期望：缺说话人/时间、重复会议、模糊表达、暂定日期、非法升级、提示注入和冲突测试。
  - 治理影响：固化会议解释与候选完整性规则。
  - 验收：只生成带原话、发言人、时间、上下文、置信度和分类依据的增量候选；重复去重、冲突单列；不自动改状态或把目标日期写成 Deadline。
  - 备注：2026-07-22 完成。新增 `meeting-ingest.v1`、会议原话/解释/冲突领域对象和 SQLite v4；会议绑定来源版本与基础状态版本，决定证据不完整时降为 `OPEN`，重复会议、原话和候选可对账，冲突只保存快照而不覆盖状态。备份清单升级为 v3，并兼容真实旧 Schema 的 v1/v2 备份。非鸿日智能纪要隔离复跑新增 0 行、正式状态 0；101 项全量测试及 2 组旧 Schema 子测试、`ruff`、编译、JSON 校验通过。

- [x] **F1.5：实现当前状态、Proposal、人工确认/驳回/替代与历史回放**
  - 优先级 / 工作量 / Lane：P0 / L / B
  - 依赖：F1.2、F1.4
  - S.U.P.E.R：S、U、P、R
  - 测试期望：Proposal 生命周期、基础版本、确认/修改/驳回/替代/重开、重复点击和事件重放测试。
  - 治理影响：状态升级、人类最终判断和修改留痕成为硬不变量。
  - 验收：无 Fox 确认事件时状态不变；旧决定通过替代关联；操作保留理由、证据、旧新状态和时间；可从事件重建当前状态。
  - 备注：2026-07-22 完成。新增 `proposal-lifecycle.v1`、SQLite v5 和 `canonical-store-port.v4`。Fox 可以逐项批准、修改后批准、驳回或携新证据重开；AI 和未授权人员不能推进生命周期。已批准内容只能通过新的 `supersede` Proposal 替代，旧状态退出当前投影但保留前后快照、事件和替代链。同状态 ID 静默覆盖、重复替代、旧版本操作都会被拒绝，事务失败整笔回滚。状态投影与 Proposal 生命周期均可从事件重建；备份清单升级 v4 并兼容 v1-v3。114 项全量测试、3 组真实旧 Schema 恢复子测试、`ruff`、编译和 JSON 校验通过；任务漂移 0，Phase 1 累计漂移保持 2。

- [x] **F1.6：实现证据、决定、开放问题关系查询、有效期与稳定回源**
  - 优先级 / 工作量 / Lane：P0 / L / C
  - 依赖：F1.3、F1.5
  - S.U.P.E.R：S、P、R
  - 测试期望：关系正反例、版本/过期、支持/反对/冲突、废案过滤和全链回源测试。
  - 治理影响：固化“相似不等于当前有效”和证据不足表达。
  - 验收：关键业务关系可查询；当前任务默认排除废案；重要结论回到原件/会议/发言人/时间/范围，证据不足明确未确认。
  - 备注：2026-07-22 完成。新增只读 `evidence-query.v1` 和 `EvidenceQueryPort`，SQLite v6 保存经 Fox 批准的可选有效期；决定和开放问题默认只返回当前未过期项，历史查询明确区分 `superseded`、`archived`、`expired`、`scheduled` 和 `unknown`。11 种关系可统一查询，会议引用可回到原话、说话人、时间、范围、来源版本和 SHA-256；自由格式或字段不全的引用返回“未确认”。来源版本更新不会破坏旧哈希证据。备份清单升级 v5 并兼容 v1-v4；126 项全量测试和 4 组旧 Schema 子测试、`ruff`、编译、JSON 与差异检查通过。真实 F1.5 数据库副本迁移到 v6 并通过 `quick_check`，其中正式决定、开放问题和关系仍为 0，未写入测试业务事实。任务漂移 0，Phase 1 累计漂移保持 2。

- [x] **F1.7：实现 Task Packet、品牌角色/模式装配与 Agent 运行留痕**
  - 优先级 / 工作量 / Lane：P0 / M / C
  - 依赖：F1.5、F1.6、F0.4
  - S.U.P.E.R：S、U、P、R
  - 测试期望：最小上下文、模式切换、角色约束、过期状态、无证据、废案隔离和运行记录测试。
  - 治理影响：固化 Task Packet 与运行时品牌协议版本。
  - 验收：只装配本轮目标、当前状态、开放项、相关证据、模式、角色、禁区和版本；运行绑定协议/模型/状态版本，AI 不能自行切换模式。
  - 备注：2026-07-22 完成。新增 `task-packet.v2`、`task-packet-assembly.v1`、`runtime-mode-switch.v2`、独立 `TaskPacketPort` 和 `runtime-run.v1`。Fox 先登记任务、品牌角色和模式；AI 不能登记任务或执行模式切换。装配器只读取任务显式引用的当前内容，过期、缺失和未确认证据进入缺口，L3 原件与 L4 历史默认不加载。Packet 以内容哈希保存为不可变快照，模式切换生成新任务版本并保留旧 Packet。每次运行从 Packet 复制角色、模式、状态版本和协议版本，再记录运行时与模型版本。SQLite 升级到 v7，备份清单升级到 v6 并兼容 v1-v5。133 项全量测试、5 组旧 Schema 子测试、`ruff`、编译和 JSON 校验通过。真实鸿日数据库副本从 v3 迁移到 v7 并通过 `quick_check`，项目版本、事件、正式状态和 Proposal 计数未变，运行表为空；没有写入测试业务事实。任务漂移 0，Phase 1 累计漂移保持 2。

- [x] **F1.8：实现本地 CLI/MCP 统一入口及 Codex/Claude 适配与模型切换**
  - 优先级 / 工作量 / Lane：P0 / L / C
  - 依赖：F1.7
  - S.U.P.E.R：P、E、R
  - 测试期望：CLI/MCP 契约、stdio、超时、取消、模型替换、凭据缺失、同 Task Packet 一致性和越权测试。
  - 治理影响：固化 AI 入口、工具允许列表与模型无私有真相规则。
  - 验收：开放状态、Task Packet、证据和 Proposal，不开放批准/任意 SQL/硬删除；Codex/Claude 使用同一 Schema；切换模型不重讲背景。
  - 备注：2026-07-22 完成。新增 `LocalAIService`、`brand-os` CLI、官方 MCP Python SDK 的低层 stdio Server，以及 `local-ai-access.v1`、`proposal-create-input.v1`、`runtime-adapter.v1`。Codex 与 Claude 指向同一项目、数据库和 Task Packet，模型必须位于 Packet 允许列表；Brand Project OS 不读取模型提供商凭据。MCP 只开放 9 个白名单工具，项目范围在启动时固定，输入拒绝额外字段；没有审批、模式切换、任意 SQL、硬删除、密钥或任意文件读取。工具默认 10 秒、最高 60 秒超时，支持取消传播。148 项全量测试、5 组旧备份 Schema 子测试、`ruff`、编译、JSON、锁文件和差异检查通过；真实鸿日 v3 数据库副本迁移到 v7 后项目版本与业务计数未变。任务漂移 0，Phase 1 累计漂移保持 2。

- [ ] **F1.9：完成公司定制 OpenWork 的离线、安全、品牌和单安装包收口**
  - 优先级 / 工作量 / Lane：P0 / L / D
  - 依赖：F1.5、F1.6、F1.8
  - S.U.P.E.R：S、P、E、R
  - 测试期望：App/Desktop/Server/Orchestrator 测试和类型检查、Electron IPC/导航/网络/权限测试、真实 `.app` 产物扫描和安装烟测。
  - 治理影响：固化唯一客户端、单安装包、上游出口、品牌和许可证边界。
  - 验收：员工只安装一个公司定制 OpenWork；OpenCode Runtime/Sidecar 随包；默认无上游遥测、Cloud、模型目录、更新和宽松外联；不另做第二个客户端。
  - 备注：2026-07-22 完成前置 OW-L0。固定 `v0.17.36@ddf3e482`，社区切片的桌面、OpenCode sidecar、Server/Orchestrator 和 macOS helper 均构建通过；桌面测试 79 通过、1 跳过，App/Server 类型检查通过。`ee/**` 不是构建依赖，但上游默认仍有 PostHog、Den/Cloud、模型目录、GitHub 更新、OpenWork AppID/协议和宽松 ATS，因此仅“有条件通过”。F1.9 仍未完成，先在正式 fork 做默认离线、内部品牌和 Electron 安全补丁，再接鸿日页面。详见 [OW-L0 技术选型记录](../phase1/openwork-ow-l0-evaluation.md)。

- [ ] **F1.10：接入鸿日业务纵切并通过 fraimz、黄金集和本地 E2E**
  - 优先级 / 工作量 / Lane：P0 / XL / E
  - 依赖：F1.1-F1.9
  - S.U.P.E.R：全部
  - 测试期望：fraimz 桌面流程、当前/证据/Proposal/模式/AI 工作、空错加载、10-20 黄金用例、冷启动、多模型、恢复和安装烟测。
  - 治理影响：记录迁移前版本、水位、已知限制和进入服务器阶段的门。
  - 验收：Fox 在唯一客户端完成八旅程；一票否决为 0；回源和恢复完整；形成 SQLite 到服务器的可校验迁移基线。
  - 备注：无。

## 阶段备注

- SQLite 是迁移前的验证权威。Phase 3 切换后退出正式写入，不形成双主。
- PostgreSQL、对象存储、OIDC 和外部组件已进入后续批准路线，但不得反向成为 F1.9/F1.10 的运行前置。
- 员工界面只在公司定制 OpenWork 中实现；不新增 Web/PWA 或第二个桌面壳。

## 完成检查

- [ ] 所有任务先记录执行遥测，再标记完成。
- [ ] 八条黄金旅程均有可复核结果。
- [ ] 七项一票否决为 0。
- [ ] 本地备份恢复与多模型切换通过。
- [ ] 本地 E2E 与安装烟测有可复核证据。
- [ ] Fox 已确认可进入服务器权威基础阶段。
- [ ] `docs/progress/MASTER.md` 已更新为 Phase 2：服务器权威基础。
