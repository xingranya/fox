# Phase 2：服务器权威基础

**目标**：建立公司服务器上的唯一正式写入面，以及身份、权限、一致性、审计和恢复能力。
**状态**：进行中，9/10，当前任务 F2.10
**任务数**：10
**自适应阈值**：标注 2 / 重计划 4 / 重定范围 6

## 任务

- [x] **F2.1：冻结服务器应用边界、配置、环境和测试基线**
  - P0 / M / Lane A；依赖 F1.10；S.U.P.E.R：S、P、E。
  - 测试：配置优先级、密钥缺失、健康检查和契约检查。
  - 验收：Brand Project OS Service、API、MCP、适配器和 OpenWork 运行层职责明确。
  - 当前说明：Phase 1 已通过。F2.1 只冻结服务器边界和测试基线，不提前迁移正式数据，也不启动双写。
  - 完成记录：新增独立服务器配置加载、`server-boundary.v1` 组件边界、`service-config.v1`/`service-health.v1` 机器契约；20 项 Phase 2 测试通过，完整回归 179 项测试通过。未启动任何 Web、数据库、对象存储、OIDC、Docker 或常驻服务。

- [x] **F2.2：实现 PostgreSQL 权威事件、审批和投影**
  - P0 / XL / Lane A；依赖 F2.1；S.U.P.E.R：S、U、P。
  - 测试：迁移、事务、事件重放、投影重建和回滚。
  - 验收：PostgreSQL 成为团队唯一可写正式状态源，领域语义与本地实现一致。
  - 完成记录：新增 PostgreSQL v1-v6 迁移、`CanonicalStorePort` 适配、迁移与命令事务锁、幂等重放、项目版本锁、人工审批、事件/投影原子提交及事件重建。真实 PostgreSQL 17 临时集群完成 10 项集成测试，完整回归 189 项及 5 组子测试通过；测试结束后数据库进程已退出。未迁移鸿日正式数据，未建立 SQLite/PostgreSQL 双写，F3.1 切换前本地 SQLite 仍是鸿日正式权威。

- [x] **F2.3：实现 S3 兼容原件版本、哈希和准入状态机**
  - P0 / L / Lane B；依赖 F2.1；S.U.P.E.R：S、U、P、R。
  - 测试：分片失败、同名异内容、哈希、版本和孤儿清理。
  - 验收：原件不可静默覆盖，数据库元数据和对象状态可对账。
  - 完成记录：新增 `object-evidence.v1`、PostgreSQL v7 上传/版本/迁移/墓碑/对账表、`ObjectStorePort`/`EvidenceMetadataPort`、boto3 S3 适配器与准入服务。桶版本控制、上传幂等、分片中断、同名异内容、SHA-256/大小/MIME/安全拒绝、内容地址复用、非法状态、孤儿清理、对象对账、人工撤销、延迟墓碑及删除/激活竞争均通过真实 PostgreSQL 17 和临时 Moto S3 HTTP 验证。Phase 2 共 43 项、完整回归 202 项及 9 组子测试通过；临时服务已退出。未读取、上传或迁移鸿日/鸿喜达正式资料。

- [x] **F2.4：实现 OIDC 登录、会话和员工身份绑定**
  - P0 / L / Lane C；依赖 F2.1；S.U.P.E.R：S、P、E、R。
  - 测试：PKCE、过期、撤权、重放和时钟偏差。
  - 验收：人工动作绑定真实员工身份；模型提供商密钥不进入业务服务。
  - 完成记录：新增 `oidc-identity.v1`、PostgreSQL v8 员工/绑定/授权事务/会话/审计表、Authorization Code + S256 PKCE、Discovery/JWKS/ID Token 校验、预登记 `(issuer, subject)` 绑定、加密令牌、刷新轮换、本地优先撤销和人工命令身份审计。Phase 2 共 74 项、完整回归 233 项及 16 组子测试通过。未连接公司 OIDC，未读取或迁移正式资料；项目 RBAC/RLS 保留给 F2.5。

- [x] **F2.5：实现项目 RBAC、保密级别和 RLS 防线**
  - P0 / L / Lane C；依赖 F2.2、F2.4；S.U.P.E.R：S、P、E。
  - 测试：角色/动作矩阵、跨项目、越权和服务角色。
  - 验收：客户端、MCP 和工作流按项目最小授权，RLS 提供纵深防御。
  - 完成记录：新增 `project-authorization.v1`、五角色九动作矩阵、P0-P3 上限、独立 AI/MCP/Workflow/System 服务身份和 PostgreSQL v9。应用服务先判权，非所有者 `NOBYPASSRLS` 运行角色再通过事务级 `SET LOCAL` 和强制 RLS 校验项目、动作与行级保密级别；服务身份在应用层和数据库层都不能评审 Proposal。Phase 2 共 83 项、完整回归 242 项及 16 组子测试通过。未连接公司身份或正式资料，未发布 HTTP 路由；详见 [F2.5 项目 RBAC、保密级别与 RLS](../phase2/project-authorization-and-rls.md)。

- [x] **F2.6：实现幂等、乐观锁和冲突差异**
  - P0 / L / Lane A；依赖 F2.2、F2.5；S.U.P.E.R：U、P、R。
  - 测试：重试、并发、陈旧版本、差异重算和事务回滚。
  - 验收：并发写入不静默覆盖；冲突返回 409、当前版本和可复核差异。
  - 完成记录：新增 `write-consistency.v1`、`write-conflict.v1`、专用幂等键冲突、`WriteConsistencyService` 和 PostgreSQL 一致读快照。100 次并发重试只提交 1 次，双请求同版本竞争只有 1 次成功；陈旧版本按批准事件重建正式状态差异，并用当前投影反查完整性。授权项目、动作、主体不匹配时在写入前拒绝，未知事务异常保持抛出并完整回滚。Phase 2 共 93 项、全量 252 项及 19 组子测试通过；临时 PostgreSQL 已退出，未读取或迁移正式资料。详见 [F2.6 幂等、乐观锁和冲突差异](../phase2/write-consistency-and-conflicts.md)。

- [x] **F2.7：实现审计、Outbox/Inbox 和后台任务边界**
  - P0 / L / Lane B；依赖 F2.2、F2.6；S.U.P.E.R：U、P、R。
  - 测试：原子提交、重复投递、乱序、死信和重放。
  - 验收：事件、审批、投影、审计和 Outbox 同事务；消费者不能直写正式表。
  - 完成记录：新增 `audit-outbox.v1`、PostgreSQL v10、追加式事件审计、按消费者 Outbox、`FOR UPDATE SKIP LOCKED` 租约、同聚合版本顺序、Inbox 去重、重试、可审计死信、死信重放和 `grant_outbox_worker_role`。消费者不写 `projects`、`events`、`proposals`、`human_actions` 或 `state_items`；正式状态查询不依赖消费者运行。专项 7 项、Phase 2 全量 101 项及 14 组子测试、完整回归 260 项及 19 组子测试通过；未启动常驻 Worker，未读取或迁移鸿日/鸿喜达资料。详见 [F2.7 审计、Outbox/Inbox 和后台任务](../phase2/audit-outbox-inbox.md)。

- [x] **F2.8：发布版本化 HTTP API 与 OpenAPI 契约**
  - P0 / L / Lane D；依赖 F2.3-F2.7；S.U.P.E.R：S、P、E、R。
  - 测试：契约、鉴权、分页、错误码、兼容窗口和限流。
  - 验收：Desktop 只调 API；人工路由与 Agent 路由分离；客户端不直连存储。
  - 完成记录：新增 `http-api.v1`、`http-error.v1`、OpenAPI 3.1 文档和可嵌入 ASGI 应用。Employee API 复用 OIDC 会话和人工命令身份，Agent API 只开放读取、证据回源、Task Packet 和 Proposal 创建，没有人工评审路由；正式写强制 `Idempotency-Key`/`If-Match` 并复用 F2.6 的 201/200/409 语义。HMAC 游标绑定项目状态版本，统一错误体覆盖 400/401/403/404/409/422/429/503；客户端没有 PostgreSQL/S3 直连能力。专项 11 项、完整回归 271 项及 19 组子测试通过；未启动常驻 Web，未连接公司 OIDC 或正式资料。

- [x] **F2.9：建立日志、指标、追踪、健康和告警**
  - P1 / L（重计划后分波次）/ Lane D；依赖 F2.7-F2.8；S.U.P.E.R：S、E、R。
  - 测试：关联 ID、脱敏、指标基数、共享限流故障和告警注入。
  - 验收：请求可追到身份、项目、状态水位和事件，日志无原文与密钥泄露；多副本限流后端故障不会静默绕过团队边界。
  - 执行波次：F2.9-A 观测契约 -> F2.9-B HTTP/健康接入 -> F2.9-C PostgreSQL 共享限流 -> F2.9-D 故障注入、告警和完整回归。
  - 完成记录：冻结 `observability.v1`、`trace-context.v1`、`metrics.v1` 和 `alert.v1`；新增结构化脱敏日志、低基数指标、HTTP span、依赖健康指标、Outbox 水位采集、告警 firing/resolved 去重和 PostgreSQL v11 共享限流。共享限流 key 只保存 SHA-256，后端不可用时返回 503 `RATE_LIMIT_STORE_UNAVAILABLE`，不回退进程内计数。专项 11 项观测测试、15 项 HTTP 测试和编译检查通过；没有启动常驻服务或接入正式资料。详见 [F2.9 可观测性、健康和告警](../phase2/observability-and-alerting.md)。

- [ ] **F2.10：完成备份恢复、故障演练和服务器阶段门**
  - P0 / L / Gate；依赖 F2.2-F2.9；S.U.P.E.R：U、E、R。
  - 测试：PostgreSQL/对象版本恢复、投影重建、哈希对账和故障注入。
  - 验收：恢复数据完整；Fox 确认内部 SLO、RPO、RTO 和部署档位。

## 完成检查

- [x] 所有任务先记录执行遥测，再更新任务和 MASTER。
- [x] F2.7 已验证事件、审批、投影、审计和 Outbox 同事务；消费者失败不影响正式状态查询。
- [x] F2.7 已验证重复投递、聚合顺序、Inbox 去重、死信和重放；Worker 角色没有正式表写权限。
- [x] PostgreSQL 是唯一正式写入源，对象存储原件可按哈希对账。
- [x] 服务账号、MCP 和工作流无法批准正式状态。
- [x] 并发冲突没有静默覆盖。
- [ ] 已完成实际恢复演练，不以“已有备份”代替恢复证据。
- [ ] F2.10 通过后才进入 Phase 3。

## 备注

本文件取代旧的 `phase-2-real-work-validation.md`。旧内容尚未开始，真实工作验证已合并到新的 Phase 4。

### F2.1 遥测

| 估算 | 实际 | 工期差 | SUPER 分数 | SUPER 变化 | 未计划依赖 | 任务漂移 |
|:---|:---|---:|:---:|:---:|---:|---:|
| M | M | 0 | 10/10 | +1 | 0 | 0 |

实现保持服务器配置与本地 `WorkspaceSettings` 分离，未引入新外部依赖，也未改变 Phase 1 领域语义。

### F2.2 遥测

| 估算 | 实际 | 工期差 | SUPER 分数 | SUPER 变化 | 未计划依赖 | 任务漂移 |
|:---|:---|---:|:---:|:---:|---:|---:|
| XL | XL | 0 | 10/10 | 0 | 1 | 1 |

本机原有 Homebrew PostgreSQL 前缀失效，额外补装 PostgreSQL 17 作为一次性集成测试运行时；没有启动常驻服务。实现新增 `psycopg` 驱动，并通过共享事务入口复用 Phase 1 v1-v6 领域语义。该调整不改变 F2.3-F2.10 的范围，Phase 2 累计漂移 1，未触发标注阈值。

### F2.3 遥测

| 估算 | 实际 | 工期差 | SUPER 分数 | SUPER 变化 | 未计划依赖 | 任务漂移 |
|:---|:---|---:|:---:|:---:|---:|---:|
| L | L | 0 | 10/10 | +1 | 1 | 1 |

生产适配新增 `boto3`，临时 S3 HTTP 集成测试新增开发依赖 Moto；两者此前未在任务依赖中锁定，因此记 1 项未计划依赖。对象 SDK、PostgreSQL 元数据和准入编排通过独立端口分层；Phase 2 累计漂移达到 2，触发标注级提醒，但未达到重计划阈值 4。

### F2.4 遥测

| 估算 | 实际 | 工期差 | SUPER 分数 | SUPER 变化 | 未计划依赖 | 任务漂移 |
|:---|:---|---:|:---:|:---:|---:|---:|
| L | XL | +1 | 10/10 | 0 | 1 | 1 |

生产身份适配新增 `PyJWT[crypto]` 和 `cryptography`，并补出授权码并发重放、单会话严格审计序号、撤销后令牌清空和管理员会话再验证等原任务未展开的安全语义，因此实际记 XL。Phase 2 累计漂移为 3，仍低于重计划阈值 4；F2.5 不扩到 HTTP 路由或客户端登录。

### F2.5 遥测

| 估算 | 实际 | 工期差 | SUPER 分数 | SUPER 变化 | 未计划依赖 | 任务漂移 |
|:---|:---|---:|:---:|:---:|---:|---:|
| L | L | 0 | 10/10 | +1 | 0 | 0 |

实现没有新增生产依赖。角色、动作、服务身份和 RLS 通过独立端口与事务上下文接入，未改写 Phase 1 领域语义，也未提前进入 F2.6 并发差异或 F2.8 HTTP 路由。Phase 2 累计漂移保持 3。

### F2.6 遥测

| 估算 | 实际 | 工期差 | SUPER 分数 | SUPER 变化 | 未计划依赖 | 任务漂移 |
|:---|:---|---:|:---:|:---:|---:|---:|
| L | L | 0 | 10/10 | +1 | 0 | 0 |

实现复用现有命令摘要、事务级 advisory lock、项目行锁、事件和正式投影，没有新增生产依赖或数据库迁移。HTTP 路由、Outbox/Inbox 和 MCP 独立命令身份分别留给 F2.8、F2.7 和 F3.6；Phase 2 累计漂移保持 3。

### F2.7 遥测

| 估算 | 实际 | 工期差 | SUPER 分数 | SUPER 变化 | 未计划依赖 | 任务漂移 |
|:---|:---|---:|:---:|:---:|---:|---:|
| L | L | 0 | 10/10 | +1 | 0 | 0 |

实现没有新增生产依赖；新增 PostgreSQL v10 和独立 `PostgreSQLOutboxMixin`，领域核心仍通过现有事件端口单向写入。Worker 只拥有运行表权限，不能写正式业务表；生产 Worker、指标/告警和 HTTP 路由分别留给 F2.8-F2.9。Phase 2 累计漂移保持 3，未触发重计划阈值；当前进入 F2.8。

### F2.8 遥测

| 估算 | 实际 | 工期差 | SUPER 分数 | SUPER 变化 | 未计划依赖 | 任务漂移 |
|:---|:---|---:|:---:|:---:|---:|---:|
| L | L | 0 | 10/10 | +1 | 1 | 1 |

实现新增直接生产依赖 Starlette，并将 HTTPX 固定为测试依赖；依赖已有 MCP 间接提供的 ASGI 生态，但为避免隐式依赖将 Starlette/HTTPX 显式写入项目配置。OpenAPI、Employee/Agent 分路、限流、分页、错误映射和兼容窗口均在进程内测试验证，未启动常驻服务。Phase 2 累计漂移为 4，达到重计划阈值；本次漂移来自 Web 适配器依赖和错误/游标契约扩展，不改变 F2.9-F2.10 顺序，后续需在 F2.9 明确多副本限流与可观测性落地边界。

### F2.9 重计划记录

F2.8 完成后，`drift_score=4` 达到 Phase 2 的重计划阈值。剩余 F2.9 不改变业务边界，但拆为观测契约、HTTP/健康接入、PostgreSQL 共享限流和故障/告警验证四个波次；F2.9 估算调整为 L，F2.10 继续等待全部波次通过。重计划前不启动常驻服务、不接入正式资料。

### F2.9 遥测

| 估算 | 实际 | 工期差 | SUPER 分数 | SUPER 变化 | 未计划依赖 | 任务漂移 |
|:---|:---|---:|:---:|:---:|---:|---:|
| L（重计划后） | L | 0 | 10/10 | +1 | 1 | 1 |

新增 PostgreSQL v11 共享限流迁移是重计划波次中才明确的依赖，记为 1 项未计划依赖。实现仍通过可替换端口，不把观测数据写回正式状态；重计划段的 `drift_score` 从 0 增至 1，未达到新的标注阈值。
