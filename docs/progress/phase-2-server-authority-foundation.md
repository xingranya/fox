# Phase 2：服务器权威基础

**目标**：建立公司服务器上的唯一正式写入面，以及身份、权限、一致性、审计和恢复能力。
**状态**：进行中，当前任务 F2.4
**任务数**：10
**自适应阈值**：标注 2 / 重计划 4 / 重定范围 6

## 任务

- [x] **F2.1：冻结服务器应用边界、配置、环境和测试基线**
  - P0 / M / Lane A；依赖 F1.10；S.U.P.E.R：S、P、E。
  - 测试：配置优先级、密钥缺失、健康检查和契约检查。
  - 验收：Brand Project OS Service、API、MCP、适配器和 OpenWork 运行层职责明确。
  - 当前说明：Phase 1 已通过。F2.1 只冻结服务器边界和测试基线，不提前迁移正式数据，也不启动双写。
  - 完成记录：新增独立服务器配置加载、`server-boundary.v1` 组件边界、`service-config.v1`/`service-health.v1` 机器契约；20 项 Phase 2 测试通过，完整回归 179 项测试通过。未启动任何 Web、数据库、对象存储、OIDC、Docker 或常驻服务。

- [x] **F2.2：实现 PostgreSQL 权威事件、审批和投影**
  - P0 / XL / Lane A；依赖 F2.1；S.U.P.E.R：S、U、P。
  - 测试：迁移、事务、事件重放、投影重建和回滚。
  - 验收：PostgreSQL 成为团队唯一可写正式状态源，领域语义与本地实现一致。
  - 完成记录：新增 PostgreSQL v1-v6 迁移、`CanonicalStorePort` 适配、迁移与命令事务锁、幂等重放、项目版本锁、人工审批、事件/投影原子提交及事件重建。真实 PostgreSQL 17 临时集群完成 10 项集成测试，完整回归 189 项及 5 组子测试通过；测试结束后数据库进程已退出。未迁移鸿日正式数据，未建立 SQLite/PostgreSQL 双写，F3.1 切换前本地 SQLite 仍是鸿日正式权威。

- [x] **F2.3：实现 S3 兼容原件版本、哈希和准入状态机**
  - P0 / L / Lane B；依赖 F2.1；S.U.P.E.R：S、U、P、R。
  - 测试：分片失败、同名异内容、哈希、版本和孤儿清理。
  - 验收：原件不可静默覆盖，数据库元数据和对象状态可对账。
  - 完成记录：新增 `object-evidence.v1`、PostgreSQL v7 上传/版本/迁移/墓碑/对账表、`ObjectStorePort`/`EvidenceMetadataPort`、boto3 S3 适配器与准入服务。桶版本控制、上传幂等、分片中断、同名异内容、SHA-256/大小/MIME/安全拒绝、内容地址复用、非法状态、孤儿清理、对象对账、人工撤销、延迟墓碑及删除/激活竞争均通过真实 PostgreSQL 17 和临时 Moto S3 HTTP 验证。Phase 2 共 43 项、完整回归 202 项及 9 组子测试通过；临时服务已退出。未读取、上传或迁移鸿日/鸿喜达正式资料。

- [ ] **F2.4：实现 OIDC 登录、会话和员工身份绑定**
  - P0 / L / Lane C；依赖 F2.1；S.U.P.E.R：S、P、E、R。
  - 测试：PKCE、过期、撤权、重放和时钟偏差。
  - 验收：人工动作绑定真实员工身份；模型提供商密钥不进入业务服务。
  - 自适应提醒：Phase 2 累计漂移 2，已达到标注阈值；F2.4 需预留身份提供商发现、时钟和撤权语义的额外复杂度，但不得降低 PKCE、重放或真实员工绑定标准。

- [ ] **F2.5：实现项目 RBAC、保密级别和 RLS 防线**
  - P0 / L / Lane C；依赖 F2.2、F2.4；S.U.P.E.R：S、P、E。
  - 测试：角色/动作矩阵、跨项目、越权和服务角色。
  - 验收：客户端、MCP 和工作流按项目最小授权，RLS 提供纵深防御。

- [ ] **F2.6：实现幂等、乐观锁和冲突差异**
  - P0 / L / Lane A；依赖 F2.2、F2.5；S.U.P.E.R：U、P、R。
  - 测试：重试、并发、陈旧版本、差异重算和事务回滚。
  - 验收：并发写入不静默覆盖；冲突返回 409、当前版本和可复核差异。

- [ ] **F2.7：实现审计、Outbox/Inbox 和后台任务边界**
  - P0 / L / Lane B；依赖 F2.2、F2.6；S.U.P.E.R：U、P、R。
  - 测试：原子提交、重复投递、乱序、死信和重放。
  - 验收：事件、审批、投影、审计和 Outbox 同事务；消费者不能直写正式表。

- [ ] **F2.8：发布版本化 HTTP API 与 OpenAPI 契约**
  - P0 / L / Lane D；依赖 F2.3-F2.7；S.U.P.E.R：S、P、E、R。
  - 测试：契约、鉴权、分页、错误码、兼容窗口和限流。
  - 验收：Desktop 只调 API；人工路由与 Agent 路由分离；客户端不直连存储。

- [ ] **F2.9：建立日志、指标、追踪、健康和告警**
  - P1 / M / Lane D；依赖 F2.7-F2.8；S.U.P.E.R：S、E、R。
  - 测试：关联 ID、脱敏、指标基数和告警注入。
  - 验收：请求可追到身份、项目、状态水位和事件，日志无原文与密钥泄露。

- [ ] **F2.10：完成备份恢复、故障演练和服务器阶段门**
  - P0 / L / Gate；依赖 F2.2-F2.9；S.U.P.E.R：U、E、R。
  - 测试：PostgreSQL/对象版本恢复、投影重建、哈希对账和故障注入。
  - 验收：恢复数据完整；Fox 确认内部 SLO、RPO、RTO 和部署档位。

## 完成检查

- [ ] 所有任务先记录执行遥测，再更新任务和 MASTER。
- [ ] PostgreSQL 是唯一正式写入源，对象存储原件可按哈希对账。
- [ ] 服务账号、MCP 和工作流无法批准正式状态。
- [ ] 并发冲突没有静默覆盖。
- [ ] 已完成实际恢复演练，不以“已有备份”代替恢复证据。
- [ ] F2.10 通过后才进入 Phase 3。

## 备注

本文件取代旧的 `phase-2-real-work-validation.md`。旧内容尚未开始，真实工作验证已合并到新的 Phase 4。

### F2.1 遥测

| 估算 | 实际 | 工期差 | SUPER 分数 | SUPER 变化 | 未计划依赖 | 任务漂移 |
|:---|:---|---:|:---:|:---:|---:|---:|
| M | M | 0 | 10/10 | +1 | 0 | 0 |

实现保持服务器配置与本地 `WorkspaceSettings` 分离，未引入新外部依赖，也未改变 Phase 1 领域语义。

### F2.2 遥测

| 估算 | 实际 | 工期差 | SUPER 分数 | SUPER 变化 | 未计划依赖 | 任务漂移 |
|:---|:---|---:|:---:|:---:|---:|---:|
| XL | XL | 0 | 10/10 | 0 | 1 | 1 |

本机原有 Homebrew PostgreSQL 前缀失效，额外补装 PostgreSQL 17 作为一次性集成测试运行时；没有启动常驻服务。实现新增 `psycopg` 驱动，并通过共享事务入口复用 Phase 1 v1-v6 领域语义。该调整不改变 F2.3-F2.10 的范围，Phase 2 累计漂移 1，未触发标注阈值。

### F2.3 遥测

| 估算 | 实际 | 工期差 | SUPER 分数 | SUPER 变化 | 未计划依赖 | 任务漂移 |
|:---|:---|---:|:---:|:---:|---:|---:|
| L | L | 0 | 10/10 | +1 | 1 | 1 |

生产适配新增 `boto3`，临时 S3 HTTP 集成测试新增开发依赖 Moto；两者此前未在任务依赖中锁定，因此记 1 项未计划依赖。对象 SDK、PostgreSQL 元数据和准入编排通过独立端口分层；Phase 2 累计漂移达到 2，触发标注级提醒，但未达到重计划阈值 4。
